// null vs undefined 

var myName = null;
console.log(myName) // null as we defined null.


console.log(typeof(myName)); // object bug in JS

var myAge;
console.log(myAge); // undefined as we didn't defined it.
console.log(typeof(myAge)); // undefined

// What is Nan

console.log("NaN is not a number,which is a variable defined in global scope and it is property of global scope")

// function isNan() used to check whether a correct number of not

